@model IEnumerable<Dari.Models.Insurance>

@{
    ViewBag.Title = "GestionInsurance";
    Layout = "~/Views/_LayoutPage1.cshtml";
}

<h2>GestionInsurance</h2>

<p>
    @Html.ActionLink("Create New", "CreateInsurance")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.idinsurance)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.website)
        </th>
        <th></th>
    </tr>

    @foreach (var item in ViewBag.insurances)
    {
        <tr>
            <td>
                @item.idinsurance
            </td>
            <td>
                @item.name
            </td>
            <td>
                @item.website
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Delete ", "DeleteInsurance", new {  id=item.idinsurance })
            </td>
        </tr>
    }

</table>
